function [X, Y] = gera_dados(dados, saida, m, r)

    load(dados);

    data = dengue_SP;

    n_data = length (data);

    X = [];
    Y = [];
    Z = [];

    for j=(m+R):n_data,
        Z = [Z; data( (j-m-):j, 1 )'];
    end

    X = Z(:, 1:m);
    Y = Z(:, m+1);

    save(saida, 'X', 'Y');

end